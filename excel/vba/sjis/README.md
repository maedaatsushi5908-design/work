# Shift-JIS 版モジュール

VBE の「ファイル → ファイルのインポート」で取り込む場合は、こちらの `.bas` を使う。

VBE のインポートは、ファイルを OS の既定文字コード（日本語 Windows では Shift-JIS）
として読む。親フォルダにある UTF-8 版をインポートすると日本語が化けるので、
インポートするならこちらを使うこと。

GitHub の画面上では、こちらのファイルは文字化けして見える。**中身を読むときは
親フォルダの UTF-8 版**を見ること。内容は同一。

コピー＆貼り付けで入れる場合は、どちらを使っても化けない。
