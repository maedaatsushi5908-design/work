# いまの状態と次にやること

新しいセッションで続きをやるときは、ここだけ読めば足ります。

## 作業時の注意（実際に起きた失敗から）

- **出力（チャットでの返答）は日本語で行うこと。**
- **VBAコードは「コピペでそのまま貼り付けて動く」形で渡すこと。**
  `.bas` ファイル1行目の `Attribute VB_Name = "..."` は VBE の
  「ファイルのインポート」専用の記法で、コード画面にそのままコピペすると
  **1行目で構文エラーになる**（実際に発生した不具合）。
  コピペ用に渡す（コピー用ページ・チャットのコードブロックなど）ときは、
  - この行を除いて渡す、または
  - 「貼り付け後にプロパティウィンドウ（F4）の (Name) を手動でモジュール名に
    変更する」手順を必ず併記する

  のどちらかを行うこと。`excel/vba/dist/*.bas` のファイル自体はインポート
  （ファイル > ファイルのインポート）でも使うので Attribute 行を残したままでよいが、
  **コピペ前提で渡す場合は必ず自分で貼り付けを再現して確認する**こと
  （見た目のレビューだけで済ませない）。

## 対象

- ブランチ `claude/excel-vba-customization-uacsmu`
- 貼り付けるもの `excel/vba/dist/M_Hasai.bas`（東白川の 06 ブックに1つだけ）
- マクロ名 `総括表の数量を転記する`

## できている工種（310セル）

| 工種 | 総括表の行 | 入る式 |
|---|---|---|
| 舗装版破砕工 | 13〜67 | `=SUMIF(…)` 109個 |
| 舗装切断工 | 9〜12 | `='試掘（舗50'!P4` 44個 |
| 殻運搬 | 77〜98 | `=給水2度!AA10` 4個（O77・O83・O87・O96）＋ `='仮配（舗'!T20` 6個（P83・P85・P87・P89・P91・P93）＋ `='給水(舗'!T19` 6個（Q83・Q85・Q87・Q89・Q91・Q93）＋ `='管工（舗50'!T27`／`='管工（舗75'!T27` 12個（R83・R85・R87・R89・R91・R93、S83・S85・S87・S89・S91・S93）＋ `='管工（舗400'!T27`／`='管工（舗600'!T27` 12個（T84・T85・T88・T89・T92・T93、U84・U85・U88・U89・U92・U93） |
| 先行路盤（発生土） | 162〜166 | `=SUMIF(…)` 35個（J〜P列 × 5行） |
| 仮復旧（再生As） | 167〜169 | `=SUMIF(…)` 36個（J〜U列 × 3行） |
| 先行路盤（材料名） | 156〜161 | `=IF(E156="再生砕石",SUMIF(…),…)` 30個（Q〜U列 × 6行） |
| 管路掘削・管路埋戻（機械施工） | 107〜132 | `='管工（土量2）50'!AE16` 16個（R・S・T・U列 × 4ブロック） |

殻運搬（現場→処分地）の P・Q・R・S・T・U列（82〜95行）は、他の列（O）とは
違って `仮配（舗`・`給水(舗`・`管工（舗50・75・400・600` の枠が「＝」区切り
でも「その1」の並びの読み替えでもうまく決められなかった（下記
KARA_DIRECT_MAP を参照）ため、総括表の行→転記元セルの対応をこの工事で
確かめてそのまま書いた36個（P・Q・R・S・T・U列 各6個）だけを追加した。
R・S・T列は R83・S83・T84 など8個がすでに手入力で正しい式だったので、
そこから行の対応を確かめて残りの行にも広げた。「15㎝以下」の予備行は
2つあり（83／84行、87／88行、91／92行）、管工（舗50・75 は前の行
（83・87・91）、管工（舗400・600 は後ろの行（84・88・92）を使う。
「15㎝超」（85・89・93行）は1行だけで全シート共通。

先行路盤（発生土）は転記元が3つある。O列は給水2度の
`□先行路盤（発生土）`（その5）、J〜N列（試掘（舗50〜600）とP列（仮配（舗）は
どちらも `種別・路盤厚｜合 計` の並び（その7。見出しの文字は試掘が
「□先行路盤（再使用）」、仮配（舗 は「□先行路盤（発生土） ※仮配水管撤去時」
で違うが、並びの形は同じ）。総括表側は同じ H列の厚さで3つとも入る。

仮復旧（再生As）も転記元が2種類ある。O列は給水2度の車道/歩道どちらか
片方（その6）。J〜N・P〜U列（試掘5枚・仮配（舗・給水(舗・管工4枚の
**全部**）は「□仮復旧工」という、舗装版破砕と同じその1の並びをもう一度
使うブロック（種別が1種類だけ続く点は先行路盤（再使用）と同じ仕組み）。
J〜N・P〜U列はどのシートにも同じ形であったので、まとめて展開できた。

先行路盤（材料名）は156〜161行の別工種。厚さでは選べず**材料名（E列）**
で転記元の枠を選ぶため、式そのものに `IF(材料名="再生砕石",…)` という
分岐を書く（その8）。給水(舗・管工4枚（Q〜U列）に同じ形の転記元があり、
まとめて展開できた。仮配（舗（P列）にはこの転記元が無いので見送り。

管路掘削・管路埋戻（機械施工）は107〜132行の別工種（その9）。厚さも
種別（As/Co）も無く、**口径だけ**で分かれる。「床掘」「土砂運搬
（現場→仮置場）」「仮置土積込」「埋戻し工」それぞれに、口径ごと
（PE50・75・400・600）の予備行が1行ずつ並ぶ。転記元は
`管工（土量2）50/75/400/600` で、R・S・T・U列がそれぞれ対応するが、
COL_MAP の `管工（舗50` などとは別のシートなので、専用の
DOBOKU_SRC_MAP・DOBOKU_MAP で列・行とセルの対応を持つ。

## 決まっていないこと

- **管路掘削・管路埋戻の J〜N・O列（人力・給水2度側）** … 今回入れたのは
  R・S・T・U列（機械施工、管工（土量2）側）の16個だけ。O105（床掘・砂質土・
  人力）は同じ仕組みなら `=給水2度!AA16` になりそうだが未確認。J・K・L・M・N列
  （試掘（土50〜600、床掘・土砂運搬・埋戻し工の各人力/機械の頭行）も
  J・M（試掘（土50・400）はすでに手入力の式が入っているが、K・L・N
  （試掘（土300・75・600）は未確認のため入れていない
- **DOBOKU_SRC_MAP・DOBOKU_MAP は工事ごとの実測値** … 管路掘削・管路埋戻の
  「口径ごとの予備行」は、床掘＝AE16、土砂運搬（現場→仮置場）＝AE19、
  仮置土積込＝AE24、埋戻し工＝AE33という4つの枠を、口径の順
  （R=PE50・S=PE75・T=PE400・U=PE600）に1行ずつ割り当てたもの。
  この行の割り振りは総括表の元の様式で決まっていて理由は分からないため、
  別の工事に持っていくときは `管工（土量2）*` の枠の並びを見て書き直すこと
- **給水2度 AA12（As歩道・現場→仮置場）の行き先** … 総括表 O列で塗られて
  いるのは 77行だけ。いま O77 は AA10（車道）のみ。`AA10+AA12` にすべきか未確認
- **162〜166行の Q・R・S・T・U列** … J〜P列以外はまだ入れていない。
  給水(舗・管工にはこの厚さ（先行路盤（発生土）の候補一覧）を持つブロック
  自体が無い（管工に出てくる「先行路盤（再使用）」の文字は試掘側の小計を
  指す見出しで、ブロックではない）。ブロックが見つからないシートは黙って
  見送る（見送り件数にも数えない）
- **仮配（舗 の「…No.2」見出し** … 仮配（舗 には先行路盤（発生土）・仮復旧工
  それぞれに見出しが2つあり（無印/No.1 と No.2）、No.2 側（AR20・AR30セル）は
  別ブック構成用の予備。この工事は No.1（無印、P25・P34セル）側だけを
  使うので、`GendoBlock`／`FukkyuBlock2` は「NO.2」を含む見出しを避けて
  探す。もし工事によって No.2 側を使うことに
  なったら、この除外条件を見直すこと
- **167〜169行の J・M・R・S・T列にあった元の手入力の式** … `□仮復旧工` の
  ブロックが見つかるようになったので、これらの列も SUMIF で上書きする
  ようになった（`='試掘（舗50'!P35` のような固定行参照だった）。値が変わる
  ところは「転記結果」シートで色が付くので、変わっていないか確認すること
- **170〜171行（仮復旧の予備行）** … H列（厚さ）が空欄なので、まだ何も入らない。
  厚さが決まったら FUKKYU_SIDE_MAP に追記が要るかもしれない
- **FUKKYU_SIDE_MAP は工事ごとの実測値** … `M_Hasai.bas` の
  `FUKKYU_SIDE_MAP = "4=歩道|5=車道|10=車道"` は、この工事で確かめた
  「厚さ→車道/歩道」の対応をそのまま書いたもの。号工の候補一覧（給水2度の
  K列＝車道／O列＝歩道）は車道・歩道で別々に詰めて並ぶため、同じ厚さでも
  どちらの側にあるかは工事ごとに違う。別の工事に持っていくときは書き直すこと
- **161行（砕石・フィルタ-）は「手入力」になる** … 先行路盤（材料名）の
  式は 再生砕石／粒調砕石 のどちらにも当たらない材料だと文字列
  「手入力」を返す。161行はどちらでもないので、Q161〜U161 は計算結果が
  「手入力」という文字になる（式が入っていないわけではない）
- **ZENKOURO_MATERIALS は工事の材料名に合わせる** … `"再生砕石|粒調砕石"`
  は総括表のE列に実際に出てくる書き方。転記元の枠のラベル（給水(舗 は
  「再生」、管工は「再生砕石」）はこの材料名に**含まれているか**で判定する
  ので多少の書き方の違いは吸収できるが、材料名自体が変わる工事では
  書き直すこと
- **KARA_DIRECT_MAP は工事ごとの実測値** … `仮配（舗`・`給水(舗`・
  `管工（舗50・75・400・600` の殻運搬（現場→処分地）は種別・舗装厚｜合計の
  並び（その1と同じ形）だが、以下/超の順番が総括表の行の並びと単純に
  対応しない（車道・歩道・Co で15㎝以下の予備行が挟まる位置が違う）ため、
  マクロには推測させず「82〜95行 → 転記元のセル」の対応をシートごとに
  この工事で確かめてそのまま `KARA_DIRECT_MAP` に書いた
  （`仮配（舗>83=T20|85=T21|87=T22|89=T23|91=Y20|93=Y21;`
  `給水(舗>83=T19|85=T20|87=T21|89=T22|91=Y19|93=Y20;`
  `管工（舗50>83=T27|85=T28|87=T29|89=T30|91=X27|93=X28;`
  `管工（舗75>83=T27|85=T28|87=T29|89=T30|91=X27|93=X28;`
  `管工（舗400>84=T27|85=T28|88=T29|89=T30|92=X27|93=X28;`
  `管工（舗600>84=T27|85=T28|88=T29|89=T30|92=X27|93=X28`）。
  管工（舗50・75・400・600 の対応は、この工事でもとから手入力で入っていた
  R83・S83・T84（T27）・R87・S87・T88（T29）・S91・T92（X27）の式から
  確かめた。「15㎝以下」の予備行は2つあり（83／84行、87／88行、
  91／92行）、管工（舗50・75 は前の行、管工（舗400・600 は後ろの行を
  使う理由は分からない（総括表の元の様式でそう割り振られていた）。
  「15㎝超」（85・89・93行）は1行だけで全シート共通。
  なお `給水(舗`・`管工（舗50・75・400・600` の T30（またはそれに相当する
  セル。`給水(舗` は T22）は転記元のセル自体が空欄（数式が無い）ので、
  Q89・R89・S89・T89・U89 は参照先が空＝0 になる。別の工事に持っていく
  ときは、これらのシート（またはそれに相当するシート）の種別・舗装厚｜
  合計の並びを見て書き直すこと

## 手元で直したものを戻すとき

`excel/vba/inbox/` に VBE からエクスポートした `M_Hasai.bas` を置く。
差分だけ見て `src` に取り込む。詳しくは `excel/vba/inbox/README.md`。
**`src` が正本**なので、VBE で直しただけだと次の `dist` 生成で消える。

## 手順

```
python3 excel/docs/verify_hasai.py    # どのセルに何が入るかを再現する
python3 excel/docs/lint_vba.py        # 名前と構文の機械チェック
python3 excel/docs/build_vba.py       # src → dist を作り直す
```

`src/M_Hasai.bas` を直したら必ず `build_vba.py` を流す。貼るのは `dist` の方。

---

# 数量計算書 転記マクロ

Excel の数量計算書の転記を自動化する。入れる前に必ず確認画面を出し、
書き込む前にバックアップを取る。

```
excel/vba/src/          保守用。ここを直す
  M_Hasai.bas             06 用。舗装版破砕を舗装厚で照合して転記（単独で動く）
  M_Util.bas              01 用の共通処理
  M_Config.bas            01 用。転記定義シート
  M_Engine.bas            01 用。転記元セルの解決
  M_Main.bas              01 用。照合・転記の入口

excel/vba/dist/         貼り付け用。src から生成する
  M_Hasai.bas             06 に貼る1ファイル（1319行）
  M_Tenki.bas             01 に貼る1ファイル（1079行）
  sjis/                   同じものの Shift-JIS 版（インポート用）
```

`src/` を直したら `python3 excel/docs/build_vba.py` で作り直す。

---

# M_Hasai － 総括表（土工事）への転記

マクロは1本だけ。

```
総括表の数量を転記する()
```

いまのところ2つの工種を扱う。**転記元の並び方が違うので、入れる式も違う。**

| 工種 | 総括表 | 入れる式 | なぜ |
|---|---|---|---|
| 舗装版破砕工 | 13〜67行 | `=SUMIF(…)` | 転記元の厚さが詰めて並ぶ |
| 舗装切断工 | 9〜12行 | `='試掘（舗50'!P5` | 厚さ区分が固定の文字で動かない |
| 殻運搬（現場→仮置場） | 77〜81行 | `=給水2度!AA10` | 摘要で1対1に決まる |
| 殻運搬（現場→処分地）O列 | 82〜95行 | `=給水2度!AA28` | 摘要で1対1に決まる |
| 殻運搬（現場→処分地）P列 | 83・85・87・89・91・93行 | `='仮配（舗'!T20` | 並びが読み替えでは決められず、行の対応を直接書いた |
| 殻運搬（現場→処分地）Q列 | 83・85・87・89・91・93行 | `='給水(舗'!T19` | 〃 |
| 殻運搬（現場→処分地）R・S列 | 83・85・87・89・91・93行 | `='管工（舗50'!T27` | 〃 |
| 殻運搬（現場→処分地）T・U列 | 84・85・88・89・92・93行 | `='管工（舗400'!T27` | 〃（15㎝以下の予備行が R・S 列とずれる） |
| 殻運搬（積込み） | 96〜98行 | `=給水2度!AA25` | 〃 |
| 先行路盤（発生土）O列 | 162〜166行 | `=SUMIF(給水2度!$U$4:$W$6,…)` | 転記元の厚さが候補一覧で詰めて並ぶ（種別は無し） |
| 先行路盤（発生土）J〜P列 | 162〜166行 | `=SUMIF('試掘（舗50'!$O$26:$O$31,…)` | その1と同じ並びだが見出しが「路盤厚」、種別は1種類 |
| 仮復旧（再生As）O列 | 167〜169行 | `=SUMIF(給水2度!$O$4:$O$6,…)` | 舗装版破砕と同じ枠を車道/歩道どちらか片方だけ使う |
| 仮復旧（再生As）他列 | 167〜169行 | `=SUMIF('仮配（舗'!$R$36:$S$39,…)` | 舗装版破砕と同じその1をもう一度使う（種別1つだけ） |
| 先行路盤（材料名） | 156〜161行 | `=IF(E156="再生砕石",SUMIF(…),…)` | 厚さでは選べず、材料名で転記元の枠を選ぶ |

```
舗装版破砕工
=SUMIF('試掘（舗50'!$O$11:$O$17,'総括表（土工事）'!$I14,'試掘（舗50'!$P$11:$P$17)
        └ 転記元の舗装厚欄      └ 総括表の厚さ欄      └ 転記元の合計欄

舗装切断工
='試掘（舗50'!P5
```

工種を増やすときは、マクロ先頭の定数に総括表の工種名と転記元のブロック
見出しを足し、`WriteAll` の振り分けに1行足す。

## 転記元の並びは3種類ある

同じ「舗装版破砕」でも、シートによって欄の並びが違う。マクロは見出しを
手がかりに**枠（種別・厚さ列・合計列・合計の幅）の一覧**を作り、
総括表の行の種別に合う枠だけを足す。

### その1　種別・舗装厚 ／ 合 計 が並ぶ形（試掘・管工・仮配・給水付替）

```
□舗装版破砕工（人力）
種別・舗装厚 | 合 計  ‖ 種別・舗装厚 | 合 計
As    4      |  0.6   ‖ Co    15     |  0.3
```

「種別・舗装厚」の1つ右が厚さ、その右で最初に来る「合 計」が数量。
枠は左から As、Co。`仮配（舗` のように Co の枠が無いシートもある。

### その2　車道／歩道 の号工が並ぶ形（給水2度）

```
□舗装版取壊工 及び 仮復旧工
車道　5号工（K:N 結合）  ‖ 歩道　5号工（O:R 結合）
K=舗装厚 | L:N=数量      ‖ O=舗装厚 | P:R=数量
```

結合の左端が厚さ、その次から右端までが数量。**どちらの枠も As**
（29行目が `As車道`、33行目が `As歩道`）なので、総括表では2枠を足す。

```
=SUMIF(給水2度!$K$4:$K$6,'総括表（土工事）'!$I31,給水2度!$L$4:$N$6)
+SUMIF(給水2度!$O$4:$O$6,'総括表（土工事）'!$I31,給水2度!$P$4:$R$6)
```

Co の枠が無いので、**Co だけの行（67行）には入れない**。

合計側の範囲が `$L$4:$N$6` と3列あるのは、転記元の合計セルが L:N で
結合されているため。SUMIF は条件範囲と同じ形の分しか見ないので、
広くても結果は変わらない。同じ理由で `仮配（舗` は `$W$10:$X$17`、
`給水(舗` は `$T$10:$U$16` と2列になる（結合が2列のため）。

### その3　舗装切断工（固定の区分）

下の節を参照。

### その4　「＝」で書かれたブロック（給水2度）

```
□As殻Co殻運搬（現場～仮置場）
  As車道 （ 0 ）        ＝  0     ← 10行
  As歩道 （ 0 ）        ＝  0     ← 12行
□掘削  ●床 堀
  砂質土（人力）（0）-（0） ＝  0     ← 16行
□As殻Co殻運搬処理（現場～処分地）  ●仮置土積込工
  As （ 0 ）           ＝  0     ← 25行
●殻運搬処理
  As車道 （ 0 ）        ＝  0     ← 28行
  As歩道 （ 0 ）        ＝  0     ← 30行
```

**「＝」の右が数量**、そこから左へ戻って最初に出てくる文字が摘要
（かっこ・ハイフン・数式・数値は飛ばす。左端から探すと行見出しを拾う）。
ブロックは次の `□` か `●` が出たところで打ち切る。

総括表の行は、工種名と副見出しで転記元のブロックを選ぶ。

```vba
Private Const KARA_MAP As String = _
    "殻運搬>現場+仮置場>□As殻Co殻運搬（|" & _
    "殻運搬>現場+処分地>●殻運搬処理|" & _
    "殻運搬>積込み>●仮置土積込工"
```

「殻運搬」は行き先で分かれるので工種名だけでは決まらない。
`～` は文字コードが揺れるので使わず、両端の語で照合する。
`仮置場→処分地`（99〜102行）は転記元が無いので入れていない。

摘要の照合は3段。

1. そのまま一致（`As・車道` → `As車道`。`・` は落とす）
2. ラベルが摘要の頭に一致（`As（仮置土）` → `As`）
3. 摘要に As / Co が無い行（`舗装版破砕` など）は種別で分かれて
   いないということなので、ブロックの**先頭行**を採る

総括表では同じ摘要が複数行（人力／15㎝以下×2／15㎝超）に分かれているが、
**転記元は摘要ごとに1つしか数量が無い**。
どの行に入れるかは**塗り分けが決めている**（O列は 77・83・87・91・96・97 行）。

### その5　路盤厚｜面 積 が並ぶ形（給水2度・先行路盤（発生土）)

```
□先行路盤（発生土）
路盤厚          ‖ 面 積
U:W(結合) | X   ‖ Y:AA(結合) | AB   ← 2枠目はまだ使わない
```

その1・その2とは逆に、**厚さ側（路盤厚）が結合で広く、合計側（面積）が
1列**。種別（As/Co）の欄が無いので枠は1つだけで、たし算もしない。

```
=SUMIF(給水2度!$U$4:$W$6,'総括表（土工事）'!$H162,給水2度!$X$4:$X$6)
```

総括表側の厚さは I ではなく **H** にある。この工種は種別（As/Co）の欄が
無いので、厚さ列（I）の1つ左で数値が見つかった列を厚さ欄とみなす。
左端（B列）は「舗装仮復旧」で他の資材（先行路盤・仮復旧）とも共通なので、
副見出し（C列）に「先行路盤（発生土）」があるかどうかで行を見分ける。

`仮配（舗` にも `□先行路盤（発生土）` の見出しはあるが、No.1/No.2 に
分かれ `路盤厚｜面 積` の並びでもないので、まだ読めない（P列は見送る）。

### その6　仮復旧（再生As）は舗装版破砕の枠を片側だけ使う

```
=SUMIF(給水2度!$O$4:$O$6,'総括表（土工事）'!$H167,給水2度!$P$4:$R$6)
=SUMIF(給水2度!$K$4:$K$6,'総括表（土工事）'!$H168,給水2度!$L$4:$N$6)
```

転記元はその2（舗装版破砕）と**同じ**「車道 5号工／歩道 5号工」の枠。
ただし舗装版破砕のように車道＋歩道を足すのではなく、**どちらか片方だけ**
を使う。号工の枠は車道・歩道とも表記が「As」なので、種別（As/Co）では
選べない。

左端（B列）は「舗装仮復旧」で先行路盤（発生土）とも共通、副見出し（C列）
は「仮復旧」だけ。ただし「仮復旧」は左端の「舗装仮復旧」にも**部分文字列
として含まれる**ため、副見出しの一致は部分一致ではなく**完全一致**で
見分ける（`HasExactLabel`）。厚さは先行路盤（発生土）と同じく I ではなく H。

車道・歩道どちらの枠を使うかは、`FUKKYU_SIDE_MAP`（厚さ→車道/歩道）で
決める。号工の候補一覧（給水2度の K列＝車道／O列＝歩道）は車道・歩道で
別々に詰めて並ぶため、**同じ厚さでもどちらの側にあるかは工事ごとに違う**。
マクロには推測させず、この工事で確かめた対応（`"4=歩道|5=車道|10=車道"`）
をそのまま書いている。給水2度は `FukkyuRef` の中で直接この枠を扱う。

厚さは **セルの数値（Value）で比べる**。実際に `.Text`（表示形式）で
比べていて「厚さ 4cm の車道/歩道が FUKKYU_SIDE_MAP にありません」と
見送りになる不具合があった。総括表のH列に㎝などの表示形式が付いていると
`.Text` は "4cm" のような文字列になり、`FUKKYU_SIDE_MAP` の "4" と
一致しなくなるため。

給水2度以外（試掘・仮配（舗・給水(舗・管工）は、この号工の枠自体が無い
ので別の転記元を探す。転記元はその1（種別・舗装厚｜合 計）だが、
**舗装版破砕とは別の場所にもう一度出てくる**もの（`FukkyuBlock2`、
見出しは「□仮復旧工」）。

```
=SUMIF('仮配（舗'!$R$36:$S$39,'総括表（土工事）'!$H167,'仮配（舗'!$T$36:$U$39)
```

このブロックは種別が「As」の1種類だけ続き（先行路盤（再使用）と同じ
`ScanSaiyouHeader` を使う。見出しの文字だけ「種別・路盤厚」ではなく
「種別・舗装厚」なので引数で渡す）、1つの行範囲の中に複数の総括表工種
（舗装版破砕工・先行路盤・仮復旧など）の候補が並ぶ。枠が2つあっても
最初の1枠だけを使う。

「□仮復旧工」は試掘5枚・仮配（舗・給水(舗・管工4枚の**全部**にあるので、
J〜N・P〜U列すべてに展開できた。仮配（舗 にはここにも「…No.2」の見出しが
あるので、`GendoBlock` と同じ理由で避ける。

167〜169行の J・M・R・S・T列には元々手入力の式（`='試掘（舗50'!P35` の
ような固定行参照）が入っていたが、このブロックが見つかるようになったので
SUMIF で上書きするようになった。値が変わったところは「転記結果」シートで
色が付くので、変わっていないか確認すること。

### その7　先行路盤（再使用）は種別が1つだけの「その1」

```
□先行路盤（再使用）
種別・路盤厚 | 合 計
再使用   10   |  0.6
```

試掘（舗50〜600 の5枚すべてにある、先行路盤（発生土）のもう1つの転記元。
その1（種別・舗装厚｜合 計）と同じ並びだが、**見出しの文字が「路盤厚」**
（舗装厚ではない）で、**種別が「再使用」の1種類だけ**（As/Co の交互では
ない）。

```
=SUMIF('試掘（舗50'!$O$26:$O$31,'総括表（土工事）'!$H162,'試掘（舗50'!$P$26:$P$31)
```

行の範囲は、種別欄が**先頭行と同じ文字で続くところまで**（As/Co のような
決まった語ではないので、その1のような固定チェックはできない）。厚さ・
合計とも幅は転記元の結合セルに合わせる（試掘は結合なしの1列、仮配（舗 は
R:S・T:U と2列結合）。総括表の同じ H列（先行路盤（発生土）の厚さ）に
対して、O列（給水2度）とは別に J〜N列（試掘）・P列（仮配（舗）にも入る。

```
=SUMIF('仮配（舗'!$R$27:$S$32,'総括表（土工事）'!$H162,'仮配（舗'!$T$27:$U$32)
```

仮配（舗 は見出しの文字が「□先行路盤（発生土） ※仮配水管撤去時」（試掘とは
違う）だが、並びの形は同じその7。総括表側は同じ厚さ1つで3つの転記元と
照合するだけなので、`GendoRef` は「見出し2種（その5用・その7用）×並び
2種（その3・その7）」を順に試す2段構え（`HasaiBlock` が舗装版破砕と
その2を順に試すのと同じやり方）。

仮配（舗 には先行路盤（発生土）の見出しがもう1つ（`…No.2`、AR20セル）
あるが、これは別ブック構成用の予備。この工事は No.1（無印、P25セル）側
だけを使うので、見出しの探索は「NO.2」を含むものを避ける。

管工（舗50 などにも「先行路盤（再使用）」の文字が出てくるが、あれは
試掘側の小計を参照する見出し文字列（`※試掘先行路盤（再使用分） 土量…`）
で、ブロックそのものではないので何も入らない。

### その8　先行路盤（材料名）は式の中の IF で材料を選ぶ

```
□先行路盤
種別・路盤厚 | 合 計 ‖ 種別・路盤厚 | 合 計
再生   10    |  0.6  ‖ 粒調   25    |  1.2
```

156〜161行の「先行路盤」（発生土でも再使用でもない）は、**厚さだけでは
転記元の枠を選べない**。同じ厚さの行が「再生砕石」だったり「粒調砕石」
だったりするため、**材料名（E列）**で枠を選ぶ必要がある。

```
=IF('総括表（土工事）'!E156="再生砕石",
    SUMIF('給水(舗'!$R$26:$S$30,'総括表（土工事）'!$H156,'給水(舗'!$T$26:$U$30),
    IF(E156="粒調砕石",
       SUMIF('給水(舗'!$W$26:$X$30,'総括表（土工事）'!$H156,'給水(舗'!$Y$26:$Z$30),
       "手入力"))
```

他の工種と違い、**マクロは材料名で式を書き分けない**。156〜161行の
どの行にも同じ式（行番号だけ違う）を入れ、どちらの枠を使うかは
Excel 側の IF が計算時に判定する。どちらにも当たらない材料（161行の
「砕石・フィルタ-」）は、式の中に直接書いた文字列 `"手入力"` がそのまま
セルの値になる（見送りではない。式は入っている）。

並びは その1 と同じ（種別・路盤厚｜合 計 が2枠）だが、種別が As/Co では
なく資材名で、しかも**転記元によってラベルの書き方が違う**
（給水(舗 は「再生」「粒調」、管工は「再生砕石」「粒調砕石」）。式に書く
比較文字列は総括表の書き方（`ZENKOURO_MATERIALS = "再生砕石|粒調砕石"`）
にそろえ、転記元の枠はラベルがその材料名に**含まれているか**で選ぶ
（「再生」は「再生砕石」に含まれる）。最初の条件だけ総括表のシート名を
付け、2つ目以降は付けない（指示された式のとおり。同じシート内の参照
なので付けなくても同じ）。

見出しは「□先行路盤（発生土）」「□先行路盤（再使用）」と違って括弧が
続かないので、部分一致で探すとその2つを拾ってしまう。括弧が続かない
ものだけを見出しとして採る。給水(舗・管工にも「…No.2」の見出しが
あるので、`GendoBlock` と同じ理由でこちらも避ける。

仮配（舗（P列）にはこの転記元（見出し「□先行路盤」）が無いので見送る
（見送り件数には数えない）。

### 殻運搬の欄は黄色ではない

77〜81・96〜97・99〜101行は、黄色ではなく**薄い橙**（テーマ色の
アクセント6）で塗ってある。凡例には出ていないが、黄色と同じく
「ここに値が入る」という指定なので、この区間だけ薄橙も入力セルとして扱う
（`IsSubCell`）。この工事では8行65セルにしか現れない。

```
82 As・車道 人力      黄=JKLMN     ← 試掘の列
83 As・車道 15㎝以下   黄=OPQRS     ← ここに 給水2度!AA28
86 As・歩道 人力      黄=JKLMN
87 As・歩道 15㎝以下   黄=OPQRS     ← ここに 給水2度!AA30
```

`●殻運搬処理` を持たない転記元（試掘・管工など）は、この工種がまだ
未対応というだけなので**黙って飛ばす**（見送りには数えない）。
どのシートで見つかったかは確認画面に出る。

## 舗装切断工はなぜ直接参照でよいのか

転記元の厚さ区分は `t≦15` `15<t≦30` `30<t≦40` と**固定の文字**で並ぶ。
舗装版破砕のように工事によって詰めて動くことがないので、行がずれない。

```
試掘（舗50 の □舗装切断工
  行3   種別・舗装厚 | 合 計  ‖ 種別・舗装厚 | 合 計
  行4   As  t≦15     |  3.60  ‖ Co  t≦15     |  0
  行5   As  15<t≦30  |  0     ‖ Co  15<t≦30  |  0
  行6   As  30<t≦40  |  0     ‖ （Co は無し）
```

ただし**ブロックの位置はシートによって違う**（試掘は4行目から、管工は10行目から）
ので、行は固定しない。マクロを流すたびに区分を照合して決め直す。

総括表は `t≦15㎝`、転記元は `t≦15` と ㎝ の有無が違うので、
㎝（U+339D）を落として突き合わせる。

## なぜ SUMIF なのか

転記元の舗装厚は、その工事に出てくる厚さだけが**詰めて並ぶ**。

```
試掘（舗50 の □舗装版破砕工
  行11  As  4cm   0.0
  行12  As  10cm  1.2      ← 5cm が無いので 10cm が2行目に来る
```

`='試掘（舗50'!P12` のように行を固定していると、別の厚さの数量を拾う。
総括表の厚さ欄（I列）と一致する行だけを合計すれば間違えない。

転記元の厚さ欄は `INDEX/MATCH` で毎回組み替わるので、
**目で見ても行がずれていることに気づけない。** そこが危ないところ。

## やっていること

1. 総括表の、いちばん左の工種名で行を仕分ける
   （「舗装版破砕」= 13〜67行、「舗装切断工」= 9〜12行）
2. **舗装厚の列**を決める。区間の行で数値が現れた回数がいちばん多い列（I列）
3. 各行の**種別**（工種名より左にある As / Co の字）を読む
4. 下の対応表にある列のうち、**黄色く塗ってあるセル**にだけ数式を入れる
5. As の行は転記元の As 側、Co の行は Co 側、As・Co の行は両方を足す

舗装版破砕は厚さ列が**数値**の行だけを見る。「計」などの文字がある行
（合計欄）は触らない。**空欄の行には入れる** — 16行目のような予備の行に
後から厚さを入れたとき、そのまま数量が出るようにするため。
SUMIF は空欄に当たらないので 0 のまま。

舗装切断工は逆で、厚さ列が**区分の文字**（`t≦15㎝`）の行を見る。

黄色は「入力セル（各数量計算書の数量を転記間違いないように！）」という
総括表の凡例そのままで、**どこに値が入るかは作成者が塗り分けている**。
推測せずにそれに従う。行26以降は口径ごとに別ブロックへ分かれるが、
塗り分けがそのまま境界になっている。

## 工事が変わったら直すところ

**マクロは総括表と転記元が同じブックにあることを前提にしている。**
東白川の `06 土工事・舗装復旧 数量計算書` がその形。
別ブックにまたがる工事（長田）は下の「他の工事から分かったこと」を参照。

まず、その工事の一式を `excel/works/<名前>/` に入れて次を流す。

```
python3 excel/docs/scan_work.py excel/works/02_nagata
```

フォルダ内の全ブックを見て、総括表と転記元が別ブックでも突き合わせる。

総括表の見出し・シート名・舗装版破砕ブロックを突き合わせて、
下の2つの下書きを出す。候補が1つに絞れない列には `★` が付く。
1件目の工事で流すと、手で確かめた `COL_MAP` と完全に一致する。

マクロの先頭にはこれだけ置いてある。

```vba
Private Const TARGET_SHEET As String = "総括表（土工事）"

Private Const COL_MAP As String = _
    "J=試掘（舗50|" & _
    "K=試掘（舗300|" & _
    ...
    "U=管工（舗600"
```

見出しの口径（`PE50-300`）とシート名の数字は1対1で対応しないので、
**マクロには推測させず、決めた結果を書いておく**。使わない列は書かなくてよい。

**シート名の数字は口径ではない。** `管工（舗50` の「50」は様式の枠の名前で、
「(75-300)新設」という見出しでも転記元は `管工（舗50` になる。
詳しくは下の「他の工事から分かったこと」を見ること。

`scan_work.py` の決め方は2段構え。

1. **同じ系統の列数と転記元シート数が同じなら、並び順どおりに組む**
   （枠の意味が順番で決まっているため。長田の管工事 5列 ⇔ 5枚）
2. 数が合わないときは、候補の少ない列から順に決めて使ったシートを外す
   （東白川の管工事 4列 ⇔ 5枚。`S(75-300)` が先に `管工（舗75` に決まる）

両方の工事で、手で確かめた `COL_MAP` と一致することを確認済み。
`給水2度` は舗装版破砕のブロックが無いので入れていない。

対応表は実行前の確認画面にそのまま出る。ブロックが見つかったかどうかも
一緒に出るので、貼り間違いはそこで気づける。

```
 J列 → 試掘（舗50    (11-17行 As=O/P Co=R/S)
 P列 → 仮配（舗      (10-17行 As=R/W Coなし)
 R列 → 管工（舗50    (16-23行 As=R/T Co=V/X)
```

転記元の**ブロックの位置は自動で探す**。「□舗装版破砕」を見つけ、その下の
「種別・舗装厚」と「合 計」の並びから列を決め、As / Co が続く行数を数える。
シートごとに列が違う（試掘は O/P、管工は R/T）ので、ここは固定できない。

行の終わりに**予備を1行足す**。次の工事で舗装厚が1種類増えても式を直さずに
済む。SUMIF は空欄に当たらないので、余分に含めても結果は変わらない。

## 入れたのに空欄に見える件

**総括表は「ゼロ値のセルにゼロを表示する」がオフ**になっている
（`showZeros = False`）。数式を入れても、結果が 0 のセルは画面上は
まったくの空欄に見える。この工事では102個中85個が 0 なので、
ぱっと見では何も入っていないように見える。

入ったかどうかは次のどれかで確かめる。

- マクロが作る **「転記結果」シート**（セル・厚さ・種別・転記元・
  前の値・後の値・入れた式の一覧。値が変わった行は色を付ける）
- セルをクリックして**数式バー**を見る
- <kbd>Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>@</kbd> で数式表示に切り替える

「転記結果」シートは要らなければ消してよい。

## この工事での結果

| 項目 | 値 |
|---|---|
| 舗装版破砕の行 | 55行（13〜67行） |
| 舗装切断工の行 | 4行（9〜12行） |
| 殻運搬の行 | 22行（77〜98行） |
| 先行路盤（発生土）の行 | 5行（162〜166行） |
| 仮復旧（再生As）の行 | 3行（167〜169行） |
| 先行路盤（材料名）の行 | 6行（156〜161行） |
| 管路掘削・管路埋戻（機械施工）の行 | 16行（107〜132行の一部） |
| 書き込むセル | **310個**（破砕 109 + 切断 44 + 殻運搬 40 + 先行路盤（発生土） 35 + 仮復旧 36 + 先行路盤（材料名） 30 + 管路掘削・管路埋戻 16） |
| うち既にリンクがあった（破砕・切断・殻運搬・管路掘削/埋戻） | 44個 → **43個は値が同じ**、1個が下記 |
| うち空だった（破砕・切断・殻運搬・管路掘削/埋戻） | 165個 → 164個は 0、1個が下記 |
| 見送り | 4個（`給水2度` O67・O91・O97、`仮配（舗` P67。いずれも Co が無い） |

殻運搬の40個は O77・O83・O87・O96（`=給水2度!AA10` など）、
P83・P85・P87・P89・P91・P93（`='仮配（舗'!T20` など、行の対応を
直接書いたもの）、Q83・Q85・Q87・Q89・Q91・Q93（`='給水(舗'!T19` など、
同じく行の対応を直接書いたもの。この工事ではどれも 0）、
R83・R85・R87・R89・R91・R93・S83・S85・S87・S89・S91・S93
（`='管工（舗50'!T27`・`='管工（舗75'!T27` など、同じく行の対応を
直接書いたもの）、T84・T85・T88・T89・T92・T93・U84・U85・U88・U89・
U92・U93（`='管工（舗400'!T27`・`='管工（舗600'!T27` など、同じく行の
対応を直接書いたもの）。このうち R83・S83・R87・S87・S91・T84・T88・T92
の8個は元から手入力で同じ式が入っていた（すでにリンクがあった側に含む）。

管路掘削・管路埋戻（機械施工）の16個は、床掘（R107・S108・T109）・
土砂運搬（現場→仮置場）（R113・S114・T115）・仮置土積込
（R118・S119・T120）・埋戻し工（R129・S130・T131）の12個が元から
手入力で正しい式が入っていた（すでにリンクがあった側に含む）。
残り4個（U110・U116・U121・U132）は空だった欄で、今回すべて
`='管工（土量2）600'!AE16` などを新たに入れた（この工事ではどれも 0）。

先行路盤（発生土）の35個（J〜P列 × 162〜166行）はすべて空だった欄。
O列は給水2度、J〜N列は試掘（舗50〜600、P列は仮配（舗の`□先行路盤（再使用）`
／`□先行路盤（発生土）`から。この工事の給水2度には 10・25・50cm しか
出てこないので、**O162・O164・O165 は数量が出て、O163（15cm）・
O166（55cm）は 0**。給水(舗・管工の Q〜U列はブロックが無いので
黙って見送る（見送り件数には数えない）。

仮復旧（再生As）の36個（J〜U列 × 167〜169行）は、O列（3個）以外は
すべて `□仮復旧工` ブロック（その1をもう一度使うもの）から。
J・M・R・S・T列の一部には元から手入力の式（`='試掘（舗50'!P35` の
ような固定行参照）が入っていたが、このブロックが見つかるようになった
ので SUMIF で上書きするようになった（上の「既にリンクがあった」の
集計にはまだ入れていない。値が変わったところは「転記結果」シートで
色が付くので、変わっていないか確認すること）。170〜171行はH列が空欄の
予備行なので、まだ何も入らない（「決まっていないこと」を参照）。

先行路盤（材料名）の30個（Q〜U列 × 156〜161行）はすべて空だった欄。
どの行にも同じ IF/SUMIF の式が入り、**161行（砕石・フィルタ-）は
計算結果が文字列「手入力」になる**（式は入っているが、Excel が
「材料名がどちらにも当たらない」と判断した結果）。仮配（舗（P列）には
この転記元（見出し「□先行路盤」）が無いので黙って見送る。

O列（給水2度）は7個。31〜34・54・55 の As・Co 行と 66 の As 行に入り、
Co だけの 67行 は見送る。
`30<t≦40` の行は総括表に無いが、この工事ではどのシートも 0 なので影響なし。

13〜25行は J・K・L・M・N の**全部**に入る（55個）。

| 行 | 厚さ | 種別 | J | K | L | M | N |
|---|---|---|---|---|---|---|---|
| 13 / 14 / 15 / **16** / 18 / 19 / 21 | 4・5・10・（空）・14・15・20 | As | ● | ● | ● | ● | ● |
| 22 / 23 / 24 / 25 | 4・8・15・20 | Co | ● | ● | ● | ● | ● |

16行目は厚さが空欄の予備行。式は入るが結果は 0。
I16 に厚さを打てば、その場で数量が出る。

## 他の工事から分かったこと（参照用）

`excel/works/02_nagata/` に **長田(花山町2丁目他)配水管取替工事** の一式を
置いてある。マクロはまだこの工事向けではないが、様式の読み方を確かめる
材料として使える。分かったことは3つ。

### 1. シート名の数字は口径ではない

`管工（舗50` の「50」は**様式の枠の名前**。どの区分を読んでいるかは、
そのシートが 延長表 のどこを参照しているかで決まる。**両方の工事で同じ。**

| 枠 | 延長表の区分 | 東白川の列 | 長田の列 |
|---|---|---|---|
| 1枠目 `（舗50` | ■新設部 | R (PE50-300)新設 | R (75-300)新設 |
| 2枠目 `（舗200` | — | （列なし） | S (200-300)新設 |
| 3枠目 `（舗75` | ■一般部・■撤去部 | S (75-300)取替・撤去 | T (75-300)取替・撤去 |
| 4枠目 `（舗400` | — | T (400-500) | U (400-500) |
| 5枠目 `（舗600` | — | U (600以上) | V (600以上) |

長田の `R（75-300)新設` に「50」の文字は無いが、転記元は `管工（舗50`。
**見出しの数字だけで決めると、新設と取替・撤去を取り違える。**

### 2. 口径で分かれない系統のシート名は変わらない

| 系統 | シート名 | 備考 |
|---|---|---|
| 仮配管 | `仮配（舗` | 両方の工事で同じ。**Co 側の欄が無い**（As だけ合計する） |
| 給水付替 | `給水(舗` | 両方で同じ。`(` は**半角**（`給水（舗` ではない） |
| 給水2度 | `給水2度` | 見出しが `□舗装版取壊工`、並びも車道／歩道の2枠。**As のみ** |

### 3. 工事によって総括表の置き場所と列数が変わる

| | 東白川 | 長田 |
|---|---|---|
| 総括表（土工事） | `06 土工事・舗装復旧` の中 | `01 総括表`（**別ブック**） |
| 管工事の列 | R・S・T・U（4列） | R・S・T・U・V（**5列**） |
| 舗装・付帯工・夜間 | V・W・X | W・X・Y（1つ右） |
| 使う口径 | PE50〜600 | 75〜300 |
| 24行目の Co | 15cm | 14cm |

長田の `COL_MAP` の下書きは `scan_work.py` が出す。ただし**総括表が別ブックに
あるので、今のマクロのままでは動かない**。別ブック参照が要るようになったら、
`git log` の `2186679` に一度作った実装がある。

## 見つかった不整合

`試掘（舗50` には 5cm が無く、`P12` は **10cm** の数量（1.2）。
ところが総括表の **J14（I列=5cm）** がその `P12` を参照している。
本来 1.2 は **J15（I列=10cm）** に入るべき値。

計の行（17行目）で合算されるので総量は合っているが、厚さ別の内訳が
1段ずれている。舗装版破砕は厚さで単価が変わるので要確認。

マクロを流すと **J14 が 0 になり、J15 に 1.2 が入る**。ここだけは値が
動くので、「計」の行が前と変わっていないことを確かめてほしい。

## うまくいかなかったとき

1つも書き込めなかった場合、マクロはどこで止まったかを出す。

```
　舗装版破砕 の行 … 55 行
　うち舗装厚の欄が使える行 … 43 行（厚さ列 I）
　うち種別(As/Co)が読めた行 … 55 行
　黄色い入力セル … 103 個
　ブロックが見つかった転記元 … 11 / 11 シート

0 になっている項目が原因です。
```

黙って何もしないことはない。

## 検証について

- `excel/docs/verify_hasai.py` … **マクロと同じ判定を Python に写したもの**
  102セル分の数式を出し、J14 が指示どおりの文字列かを確かめる
- `excel/docs/scan_work.py` … 工事フォルダから `COL_MAP` の下書きを作る
- `excel/docs/lint_vba.py` … 名前と構文の機械チェック
  - 予約語を変数名に使っていないか（`Dim any As Boolean` は構文エラー）
  - **手続き名と同じ名前の変数**（`Function LastRow()` と `Dim lastRow` は
    VBA が大文字小文字を区別しないため衝突する）
  - VBA / Excel の関数と同じ名前の変数（`Dim Val As String` など）
  - **モジュール内の手続きを、引数の数を間違えて呼んでいないか**
    （引数を1つ増やしたのに古い呼び出しが残っている、など）
  - **関数の戻り値への直接添字**（`Split(s, "|")(1)`。要素が足りないと
    実行時に落ちる。VBA の `And` は左が偽でも右を評価するので、
    `If n = 2 And Split(s,"|")(1) = …` では守れない）
  - モジュールレベルの宣言が手続きより後ろに来ていないか
  - Sub / Function と End の対応、コード行の全角スペース

```
python3 excel/docs/verify_hasai.py                    # 東白川の06
python3 excel/docs/scan_work.py excel/works/02_nagata  # 長田の様式を読む
python3 excel/docs/lint_vba.py
```

---

# M_Tenki － 01 総括表への転記

`01 総括表` の数量を `02`〜`08` の各数量計算書から拾って入れる。
マクロは「定義シートを作成」「照合実行」「転記実行」の3本。
検証は `excel/docs/verify_mapping.py`（26/26 一致）。
